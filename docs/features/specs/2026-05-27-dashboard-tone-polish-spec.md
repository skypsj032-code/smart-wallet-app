# Dashboard Tone Polish

- Why: The dashboard structure is stronger now, but several English chips and a hardcoded `No budget` string still break the `다정가계부` tone.
- Change: Convert home status-chip labels and budget fallback copy to Korean, and replace the repeat-suggestion CTA's fixed width with a content-driven layout.
- Out of scope: Rewriting dashboard headlines, changing feature order, or redesigning recurring/budget logic.
