# Today Loop Action Focus

- Why: The home screen already has a dedicated recent-transactions card with a timeline entry point, so `Today Loop` should stop duplicating that action.
- Change: `Today Loop` keeps only the quick-entry action and becomes a single-purpose "do something now" card.
- Out of scope: Rewriting the Today Loop copy, changing transaction timeline behavior, or redesigning the recent-transactions card.
