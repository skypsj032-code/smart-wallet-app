- 변경: `DashboardSummary`에 `MonthlySpendPace` 모델을 추가하고, 이번 달 누적 지출과 날짜 진행률을 바탕으로 월말 예상 지출과 페이스 상태를 계산하게 했다.
- 변경: 홈 반복지출 카드 다음에 `이번 달 소비 페이스` 카드를 추가해서, 현재 속도와 월말 예상치를 설명형 문구로 보여주게 했다.
- 변경: 예산이 있는 경우 `예산 대비 n% 예상`, 예산이 없는 경우 `월말 예상 지출` 배지로 문구를 분기했다.
- 검증: `dashboard_summary_provider_test.dart` 에서 steady / overspending 계산을 고정했고, `dashboard_screen_test.dart` 에서 소비 페이스 카드 노출과 핵심 문구를 검증했다.
- 차이: 처음에는 예산 카드 안에서 해결하려 했지만, 반복지출 다음에 별도 보상 카드로 두는 편이 편한가계부식 즉시 보상과 더 잘 맞아서 분리했다.
