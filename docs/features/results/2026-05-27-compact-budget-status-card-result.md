- 변경: `예산 상태` 카드에 `budget-status-card` key를 추가하고, 진행률과 남은 금액을 바로 읽을 수 있는 수치 줄을 붙였다.
- 변경: 큰 headline 중심 대신 `예산 대비 퍼센트 + 남은 금액 + 기존 설명`으로 읽히게 해서, 홈 상단 보상 카드들과 역할이 덜 겹치게 했다.
- 검증: `dashboard_screen_test.dart` 에서 예산 카드 노출과 `86%`, `70,000` 수치 표시를 고정했다.
- 차이: 완전한 레이아웃 교체보다, 인코딩 리스크를 피하기 위해 기존 카드에 핵심 수치만 더해 보조 확인 패널로 먼저 압축했다.
