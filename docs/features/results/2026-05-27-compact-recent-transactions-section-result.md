- 변경: 홈 `최근 거래` 섹션을 최근 3건만 보여주는 컴팩트 리스트로 줄이고, 하단 `전체 보기` 버튼으로 타임라인으로 넘기게 했다.
- 변경: 카드 key와 거래 타일 key를 추가해서 홈 확인 패널로서의 동작을 테스트 가능하게 만들었다.
- 검증: `dashboard_screen_test.dart` 에서 4건 이상이 들어와도 홈에는 3건만 노출되고, `전체 보기` 버튼이 보이는지 고정했다.
- 차이: 단순 길이 축소가 아니라, 홈에서 `마지막 점검` 역할만 맡고 상세 확인은 타임라인으로 넘기는 방향으로 역할을 분리했다.
