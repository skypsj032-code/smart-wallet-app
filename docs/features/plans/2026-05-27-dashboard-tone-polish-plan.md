- Files: `lib/features/dashboard/presentation/dashboard_screen.dart`

- Replace dashboard chip labels with Korean product tone.
- Change the budget fallback text from `No budget` to Korean copy.
- Remove the fixed 96px width from the repeat-suggestion CTA and make the button size itself from content while keeping a stable minimum height.

- Verify: `./flutterw.bat test --no-pub test/features/dashboard/dashboard_screen_test.dart`
